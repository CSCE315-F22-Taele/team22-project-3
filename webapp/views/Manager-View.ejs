<!DOCTYPE html>
<head>
    <title>Inventory</title>
    <link rel="stylesheet" href="css/style.css"></link>
    <style>@import url('https://fonts.cdnfonts.com/css/western-cowboy');</style>
</head>

<header>
    <nav>
        <b>|</b>    <a href="/ ">Home</a>
        <b>|</b>    <a href="/online-order">Online Order</a>
        <b>|</b>    <a href="/server-view">Server View</a>
        <b>|</b>    Manager Page
    </nav>
</header>

<body>
    <div class = "wrapper">
        <h1>Inventory</h1>
        <div class = "menu" >
            <table>
                <tr class = "table-title">
                    <td>ID</td>
                    <td>Name</td>
                    <td>Current Count</td>
                    <td>Max Count</td>
                    <td>Price</td>
                    <td>Menu Item</td>
                    <td>Protein</td>
                </tr>
                <% for (var i in items) { %>
                    <tr>
                        <td><%=items[i].food_id%></td>
                        <td><%=items[i].food_name%></td>
                        <td><%=items[i].current_count%></td>
                        <td><%=items[i].max_count%></td>
                        <td><%=items[i].sell_price%></td>
                        <td><%=items[i].is_menu_item%></td>
                        <td><%=items[i].is_protein%></td>
                    </tr>
                <% } %>
            </table>
        </div>
    </div>
</body>

